server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://192.168.50.142:3306/GPT?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
    username: root
    password: Aa8224084.
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

zhipu:
  api-key: ${ZHIPU_API_KEY:}

